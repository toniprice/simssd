url: https://toniprice.github.io/simssd/

# See https://pkgdown.r-lib.org/reference/build_site.html#development-mode to
# generate different sites for the development and released versions of the
# package

development:
  # See https://pkgdown.r-lib.org/reference/build_site.html#development-mode
  mode: unreleased

template:
  bootstrap: 5
  bootswatch: litera
  theme: arrow-light

  bslib:
    primary: "#287db2"
    # base_font: {google: "Roboto"}
    # heading_font: {google: "Roboto Slab"}
    # code_font: {google: "M PLUS Code Latin"}

navbar:
  # NB: This 'type' declaration is necessary to style the navbar correctly
  type: default
  # logo:
  #   src: man/figures/logo.png

articles:
- title: Basics
  # Note: Remove the 'navbar: ~' for an index page instead of a dropdown
  #       See https://pkgdown.r-lib.org/reference/build_articles.html
  navbar: ~
  # desc: Basic usage
  contents:
  - simssd
  - intro
  - parameters
  - approach
